# Connector worker

Read `../COMMON-WORKER.md`, the filled packet, and `packages/connectors/AGENTS.md`. Scope the lane to one named provider/source behavior with a frozen shared contract.

Load `orient-repository`, `connector-work`, `security-privacy-review`, `elegance-review` for code/review, and `handoff-work`; add `implement-change` for code and `provenance-invalidation` when source revisions/deletion affect projections. Use `test-strategy` for coverage design and `diagnose-failure` when the cause is unknown.

1. Produce a dated primary-source packet for sanctioned authentication, scopes, setup, history limits, cursors, deletion, quotas, and honest fallback. Research access does not authorize account enrollment or paid calls.
2. Name the capability accurately: live sync, loopback, watched folder, or export import. Reuse core identity, event, grant, secret-reference, sensitivity, and checkpoint contracts.
3. Keep normalization deterministic and pagination/timeouts bounded. Advance checkpoints only after accepted evidence is durable. Implement registry claims only after the real adapter and conformance pass.
4. Run synthetic offline shared conformance and provider tests: missing credentials, repeated backfill, restart/resume, stable identities/hashes, declared tombstone behavior, revoke, purge planning, redaction, and bounds.
5. Report fixture proof and live-account proof separately. If live access is unavailable, retain the exact limitation; do not fabricate qualification or substitute another route.

Shared registry, connector interface, core grant policy, operator credentials, and package lockfiles remain with their named owners unless the packet assigns them here.
