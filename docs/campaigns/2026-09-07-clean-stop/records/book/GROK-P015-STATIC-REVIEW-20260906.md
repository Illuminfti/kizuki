# P015 static review — changes required

Reviewed candidate `34d02119014e7fd071e5ab3709fd65b7c0a5e065`, parent/base `f57acb3046e6bdd7cbee3b260cdfe6114b8f58c7`, tree `27247ce740c3473ca329727927e9f37ade34a034`, in the clean private checkout `PRIVATE_FLEET/code-repos/P015`. All 13 changed files are within the existing 17-path P015 allowance. Contract: `P014-SOURCE-SURVIVOR-LINEAGE-CONTRACT.md`, SHA-256 `0d2f1d547a8b7a92354c4fc8ee34b6f18f70fb9e91db1bf0efb0c539e626bfaf`.

This is an independent static source/diff review. No candidate code, tests, vulnerability reproduction, or resource-bound reproduction was executed by this reviewer; no candidate file was edited. Root owns sealed test execution and its original failure receipts. This report supplies neither test PASS nor acceptance, merge, release, or qualification credit.

## Material findings

1. **P1 — every newly serialized source-erasure intent uses the wrong call signature.** `packages/core/src/canon/source-erasure-intent.ts:194–198` defines `writeIntent(db, intent)` but its calls at lines 260 and 271 pass only `next`. The unused first parameter receives the intent; the second parameter is undefined, so serialization supplies no JSON string to the byte-length check. This statically identifiable arity error prevents normal v2 staging and legacy live-preimage intent upgrades. Correct the helper signature or both calls without removing the 256 KiB UTF-8 bound. Preserve the original candidate's failure receipts and obtain fresh corrected-head evidence.

2. **P1 — checkpoint traversal omits the shared 128-generation guard.** `packages/core/src/canon/authority.ts:178–180` checks the limit in `state()`, but checkpoint recursion at lines 282–287 calls `fromCheckpoint()` directly after extending `seen`; that function does not enforce the limit. The separate portable binding validator (`packages/core/src/ledger/canon-source-survivor-lineage.ts:267–286,331–352`) counts checkpoint child IDs, while its terminal predecessor and revert target are not added to that shared generation accounting. Both paths must enforce the frozen shared visited/depth budget for each participating receipt, checkpoint edge, and revert target. Preserve the resolver's bounded distinct-row accounting and unavailable/no-positive-basis outcome on refusal. This finding follows the control flow only; no over-limit history was constructed or run.

3. **P2 — portable restore does not enforce checkpoint-row uniqueness.** `packages/core/src/ledger/canon-source-survivor-lineage.ts:456–457` delegates restore insertion to the runtime idempotent helper. That helper returns for an identical existing row at lines 363–370. `packages/core/src/export.ts:2656–2666` counts input rows and validates the resulting graph, but never compares unique inserted child IDs with that count. Restore therefore lacks the contract's stream uniqueness/count correspondence check. Give restore a strict insertion/duplicate check while preserving narrowly scoped runtime retry idempotence. All stream validation must remain inside the existing transaction and before derived rebuilding/publication. No malformed backup was constructed or replayed.

4. **P1 — archived mixed-source survivors are incorrectly required to have live lineage.** `packages/core/src/canon/apply.ts:741–764` requires a positive current-path basis for every non-null source replacement, including archive paths. `packages/core/src/canon/source-erasure-intent.ts:65–74` classifies edit receipts without testing whether the path is live; staging at line 233 then invokes `liveSurvivorLineage()`, whose path check rejects archives at lines 107–108. The existing caller expressly traverses archive preimages and can retain independent claims there (`packages/core/src/ledger/source-canon-erasure.ts:142–209`). Such archive edits must continue under their established validation with `lineage = null`; they cannot satisfy the new live-page predicate. The separate finish predicate in `canon/apply.ts:910–920` already distinguishes live paths, so staging, validation, retry/recovery, and finish need one consistent classification. Preserve live survivor positivity and never use archive hash coincidence to grant live evidence. This remains a normal-lifecycle regression after fixing finding 1.

## Whole-scope observations

- The new migration allocates version 20 and creates the eight-field STRICT table with explicit type/value/byte/hash constraints and RESTRICT receipt references. It does not backfill historical checkpoints. Normal open and public health checks invoke the new assertion; serving does not create the table.
- Live staging reads the owned preimage, requires a positive basis for the exact original receipt and current path/hash, validates child/predecessor chronology, and freezes the effective predecessor tier. The survivor tier is selected from retained claims. New intent reads project a bounded byte string before fatal UTF-8 decoding. Raw protective authority is not used to manufacture a live basis.
- Completion retains the receipt stream capability through the immediate SQLite transaction that handles the actual receipt, checkpoint, holds/index, and intent deletion. Partial pair conflicts are refused. Legacy postimages without lineage remain incomplete. Finding 4 prevents the required legacy archive continuity.
- Export writes the always-present v20 lineage stream in the same owned immediate snapshot as receipts. It uses SQL byte projections, BINARY child-ID order, per-row and aggregate row bounds, and the existing manifest hashes/counts. Pending source-erasure intents block export. Restore follows receipt insertion inside the all-stream transaction and validates the graph before derived rebuilding. Finding 3 leaves uniqueness incomplete.
- Current-format required-stream absence is refused; legacy-format backups carrying the new stream are refused; supported backups without it receive an explicit warning and no invented checkpoints. Completed purge history deliberately remains compatible with ledger 19 and 20.
- `packages/core/test/source-grants.test.ts` is unchanged from f57, including the genuine surviving-B serving assertions at f57 lines 1396 and 1563. The corrected raw-unrecorded negative assertion was not weakened by this diff.

## Validation suitability and remaining integration work

All added/modified P015 tests are ordinary synthetic lifecycle, migration/backup compatibility, interruption recovery, or negative input validation suitable for root's sealed execution. The new inconsistent-checkpoint case expects refusal in a temporary database; it does not reproduce a successful exploit. No added predecessor-bypass or resource-exhaustion reproduction was found. This judgment applies to P015 changed cases and does not expand execution authorization for unrelated preexisting fixtures.

The new test named “ordinary purge, revert and undo-of-undo reach a source-survivor checkpoint” performs both undo operations before creating the checkpoint, and only the ordinary purge afterward (`source-survivor-lineage.test.ts:191–234`). Its assertions do not establish the contract's separate post-checkpoint revert/undo-of-undo behavior. The new migration test preserves a v1 record, and the legacy recovery test exercises postimage refusal; a valid v1 preimage upgrade still needs ordinary lifecycle evidence. Complete those acceptance cases through normal public seams, without vulnerability/resource reproductions or relaxed assertions.

Two disclosed integration items remain outside the frozen P015 write allowance: `packages/core/test/agent-enrollment-backup.test.ts` pins schema 19, and `packages/core/src/ledger/integrity.ts` itself does not assert the v20 table. The latter is relevant because enrollment's immutable preview directly calls `assertLedgerSchema()` (`packages/core/src/agents/enrollment.ts:340`) rather than the new wrapper. Root must assign any changes to those paths explicitly; this handoff does not silently broaden the 17-path ownership. Worker should report these integration dependencies honestly.

Resume the same P015 owner on the reviewed head with the unchanged contract and path allowance. Preserve original result/test receipts, correct the four findings, finish the stated ordinary lifecycle evidence, and return the new exact head for independent review and root's qualified tests. All acceptance gates remain open.
