# P015 correction handoff

Continue from clean candidate `34d02119014e7fd071e5ab3709fd65b7c0a5e065`, based on `f57acb3046e6bdd7cbee3b260cdfe6114b8f58c7`, under the same P015 owner and exact existing 17-path allowance. The frozen `P014-SOURCE-SURVIVOR-LINEAGE-CONTRACT.md` remains unchanged (SHA-256 `0d2f1d547a8b7a92354c4fc8ee34b6f18f70fb9e91db1bf0efb0c539e626bfaf`). Root preserves original candidate and test-failure receipts.

1. Fix the `writeIntent` arity error without weakening the serialized UTF-8 size bound. Both new v2 staging and valid legacy preimage upgrade must serialize the intended object.
2. Apply the shared 128-generation/visited accounting to resolver checkpoint recursion and portable binding validation, including terminal predecessors and revert targets. Preserve 4,096 distinct receipts per live page and its overflow sentinel. Refusal remains unavailable/no positive basis. Use static defensive review and bounded ordinary validation; do not construct over-limit or vulnerability reproductions.
3. Enforce unique child IDs in the portable stream before publication. Restore must reject duplicates and keep row counts consistent with stored rows. Preserve exact runtime retry semantics separately; do not weaken graph, reference, chronology, bound, or manifest checks.
4. Classify live survivor versus archive/deletion consistently across authority selection, intent staging/validation, retry/recovery, and finish. Only genuine current non-archive survivors require and receive lineage. Established archive/deletion handling continues with null lineage. Never use archive matching hashes as live positive authority.

Finish ordinary positive evidence for genuine surviving B, mixed-source archive continuity, valid legacy preimage upgrade, repeated source erasure, post-checkpoint ordinary revert/undo-of-undo, interrupted exact completion, and v20 backup/purge-history portability. Preserve both genuine-B assertions and the raw-unrecorded negative fixture unchanged. Added/modified tests were statically screened as ordinary synthetic validation suitable for root's sealed runner; submit corrected-head test requests under that existing protocol.

Do not edit unowned `ledger/integrity.ts` or `agent-enrollment-backup.test.ts`. Report their schema-20 integration dependencies to root. Do not change the contract, migration allocation, model/release claims, independent review gates, or other owners' paths. No candidate code/test execution was performed by this reviewer. Full rationale and exact source references are in `GROK-P015-STATIC-REVIEW-20260906.md`; the paired JSON receipt preserves the full allowance and identity.
