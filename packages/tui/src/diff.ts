/**
 * The line diff moved into core when the correction path began reporting the
 * rewrite it performed: one implementation, two readers.
 */
export { diffLines } from "@kizuki/core";
export type { DiffLine } from "@kizuki/core";
