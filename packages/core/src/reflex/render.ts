import type { ReflexReport } from "./types";

const escape = (value: unknown): string => String(value)
  .replace(/[\u0000-\u0008\u000b\u000c\u000e-\u001f\u007f-\u009f\u202a-\u202e\u2066-\u2069]/gu, "")
  .replace(/[&<>"']/g, char => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[char]!);
const style = `
:root{color-scheme:dark;--bg:#0b0e15;--panel:#131823;--line:#2d3548;--text:#eff3ff;--muted:#b1bace;--mint:#9ef6d1;--violet:#c4b2ff;--red:#ffb4be;--amber:#ffd397}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:16px/1.6 system-ui,-apple-system,sans-serif}main{overflow-wrap:anywhere;max-width:1240px;margin:auto;padding:42px 28px 80px}a{color:var(--mint);text-underline-offset:4px}a:focus-visible,summary:focus-visible{outline:3px solid var(--mint);outline-offset:5px}header{border-bottom:1px solid var(--line);padding-bottom:32px}.eyebrow{letter-spacing:.2em;font-size:12px;font-weight:700;color:var(--mint)}h1{font-size:clamp(48px,10vw,104px);line-height:1;letter-spacing:-.065em;margin:22px 0}h1 span{color:var(--violet)}h2{font-size:24px;letter-spacing:-.025em;margin:38px 0 16px}p{margin:12px 0}.lede{max-width:700px;font-size:20px;color:var(--muted)}.meta,.muted{color:var(--muted)}.meta{font-size:13px;overflow-wrap:anywhere}.nav{display:flex;gap:24px;flex-wrap:wrap;margin-top:24px}.badge{display:inline-block;border:1px solid var(--line);border-radius:6px;padding:3px 9px;font-size:12px;font-weight:700;letter-spacing:.04em;text-transform:uppercase}.conflicted,.contradicted{color:var(--red)}.unknown,.unclear{color:var(--amber)}.supported,.supports{color:var(--mint)}.contradicts{color:var(--red)}.irrelevant{color:var(--muted)}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin:28px 0}.stat,.notice,details{background:var(--panel);border:1px solid var(--line);border-radius:12px}.stat{padding:20px}.stat strong{display:block;font-size:36px;line-height:1.2}.stat span{font-size:13px;color:var(--muted)}.notice{padding:18px 22px;border-left:3px solid var(--amber)}details{margin:12px 0;padding:0 20px}summary{cursor:pointer;padding:20px 0;font-weight:650}summary .badge{margin-right:12px}.detail{padding:0 0 22px}.statement{font-size:19px}.chips{display:flex;gap:8px;flex-wrap:wrap}.chips a{font:12px ui-monospace,monospace;overflow-wrap:anywhere}table{width:100%;border-collapse:collapse;background:var(--panel)}.scroll{overflow:auto;border:1px solid var(--line);border-radius:12px}th,td{padding:14px 18px;border-bottom:1px solid var(--line);text-align:left;vertical-align:top}thead th{font-size:12px;color:var(--muted);white-space:nowrap}tbody th{min-width:220px;max-width:360px;font-weight:500}td{font-size:13px;white-space:nowrap}caption{text-align:left;padding:14px 18px;color:var(--muted);font-size:13px}.sources{display:grid;gap:12px}.source{min-width:0;border-top:1px solid var(--line);padding:16px 0;scroll-margin-top:20px}.source code{font-size:12px;overflow-wrap:anywhere}code{font-family:ui-monospace,monospace;overflow-wrap:anywhere}footer{margin-top:40px;border-top:1px solid var(--line);padding-top:24px;color:var(--muted);font-size:13px}@media(max-width:650px){main{padding:26px 16px 48px}.stats{grid-template-columns:repeat(2,1fr)}.stat{padding:16px}summary .badge{display:table;margin-bottom:9px}h2{margin-top:28px}}@media print{body{background:white;color:black}.stat,.notice,details,table{background:white;color:black}.muted,.meta{color:#444}main{max-width:none}.scroll{overflow:visible}}
`;
/** Offline, script-free report. No external fonts, analytics, requests, or raw source bodies. */
export function renderReflexHtml(report: ReflexReport, options: { demo?: boolean } = {}): string {
  const sources = new Map(report.evidence.map((e, i) => [e.event_id, i]));
  const references = (ids: readonly string[]): string => ids.map(id => {
    const index = sources.get(id);
    return index === undefined ? escape(id) : `<a href="#source-${index}">E${String(index + 1).padStart(2, "0")}</a>`;
  }).join(" · ") || "None";
  const safeClass = (value: string): string => ["supported", "contradicted", "conflicted", "unknown", "supports", "contradicts", "irrelevant", "unclear"].includes(value) ? value : "unknown";
  const unresolved = report.findings.filter(f => f.verdict !== "supported").length;
  const cards = report.findings.map(f => `<details${f.verdict === "supported" ? "" : " open"}>
<summary><span class="badge ${safeClass(f.verdict)}">${escape(f.verdict)}</span>${escape(f.statement)}</summary>
<div class="detail"><p class="meta">${escape(f.id)} · ${escape(f.importance)} importance</p><p>${escape(f.next_step)}</p>
<p class="chips">Supporting: ${references(f.supporting)}</p><p class="chips">Contradicting: ${references(f.contradicting)}</p><p class="chips">Unresolved: ${references(f.unresolved)}</p></div></details>`).join("");
  const rows = report.findings.map(f => `<tr><th scope="row">${escape(f.statement)}</th>${report.evidence.map(e => {
    const cell = report.matrix.find(c => c.assumption_id === f.id && c.event_id === e.event_id);
    return cell === undefined ? `<td class="muted">Not assessed</td>` : `<td class="${safeClass(cell.relation)}">${escape(cell.relation)}<br><span class="meta">confidence ${escape(cell.confidence.toFixed(2))}</span></td>`;
  }).join("")}</tr>`).join("");
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'">
<meta name="referrer" content="no-referrer"><title>Kizuki Reflex — evidence before action</title><style>${style}</style></head><body><main>
${options.demo === true ? '<aside class="notice"><strong>Synthetic demo.</strong> These are scripted fixture judgments, not live Jev results or an accuracy benchmark.</aside>' : ""}
<header><div class="eyebrow">KIZUKI / EVIDENCE BEFORE ACTION</div><h1>RE<span>FLEX.</span></h1>
<p class="lede">See what your plan assumes. Find where your memory disagrees.</p>
<span class="badge">${report.status === "assessed" ? "Assessment complete" : "Assessment unavailable"}</span>
<p class="meta">${escape(report.snapshot.at)} · ${escape(report.snapshot.principal)} · ${escape(report.model ?? "No model result")}</p>
<nav class="nav" aria-label="Report sections"><a href="#findings">Findings</a><a href="#matrix">Evidence matrix</a><a href="#sources">Source lineage</a></nav></header>
<div class="stats"><div class="stat"><strong>${unresolved}</strong><span>Assumptions to resolve</span></div><div class="stat"><strong>${escape(report.coverage.examined_events)}</strong><span>Sources examined</span></div><div class="stat"><strong>${escape(report.metrics.questions)}</strong><span>Atomic judgments requested</span></div><div class="stat"><strong>${escape(report.metrics.dispatched_batches)}</strong><span>Model batches dispatched</span></div></div>
<aside class="notice"><strong>Not an execution permit.</strong> This checks selected sources, not the whole world. A supported result is not a truth guarantee. Permissions and evidence must be revalidated before action.${report.reason === null ? "" : `<p>Unavailable reason: <code>${escape(report.reason)}</code>. Do not treat missing results as approval.</p>`}</aside>
<section id="findings"><h2>01 / Resolve these assumptions</h2>${cards}</section>
<section id="matrix"><h2>02 / The evidence matrix</h2><div class="scroll" tabindex="0" role="region" aria-label="Scrollable evidence matrix"><table><caption>One typed judgment per assumption–source pair. Confidence is a model score, not a calibrated probability of truth.</caption><thead><tr><th scope="col">Assumption</th>${report.evidence.map((_, i) => `<th scope="col"><a href="#source-${i}">E${String(i + 1).padStart(2, "0")}</a></th>`).join("")}</tr></thead><tbody>${rows}</tbody></table></div></section>
<section id="sources"><h2>03 / Source lineage</h2><p class="muted">Only readable source metadata appears here. Source bodies are deliberately not embedded.</p><div class="sources">${report.evidence.map((e, i) => `<article class="source" id="source-${i}"><span class="badge">E${String(i + 1).padStart(2, "0")} · ${escape(e.eligibility)}</span><p><code>${escape(e.event_id)}</code></p><p class="meta">Occurred ${escape(e.occurred_at)} · ${escape(e.sensitivity)}</p><p class="meta">Body SHA-256: <code>${escape(e.sha256 ?? "Not loaded")}</code></p></article>`).join("") || "<p>No readable sources.</p>"}</div></section>
<footer><p>Policy <code>${escape(report.policy)}</code> · Snapshot <code>${escape(report.snapshot.digest)}</code></p><p>Freshness upper bound: ${escape(report.snapshot.valid_until)}. Revocation or evidence changes invalidate this sooner. This digest is not a signature or authorization token.</p><p>Provider-reported tokens: ${escape(report.metrics.input_tokens)} input / ${escape(report.metrics.output_tokens)} output. Assessment time: ${escape(report.metrics.elapsed_ms)} ms. These are operational counts, not a live-model accuracy benchmark.</p><p>Private, ephemeral report. Export only deliberately; saved copies do not participate in vault purge. No scripts, external assets, or telemetry.</p></footer>
</main></body></html>`;
}
