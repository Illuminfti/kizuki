{
  "name": "kizuki-workspace",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "verify": "bash scripts/verify.sh"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/bun": "^1.3.0"
  }
}
